# graphql-api：GraphQL API

## 文件位置

`.claude/skills/graphql-api/SKILL.md`

## 适用场景

只用于 GraphQL schema、query、mutation、resolver、dataloader、授权、分页、schema 演进和 GraphQL 错误行为。

## 示例提问

```text
请为订单列表设计 GraphQL query 和 resolver。
```
